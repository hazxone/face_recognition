securityDefinitions:
  Bearer:
    type: apiKey
    name: Authorization
    in: header

security:
  - Bearer: []

definitions:
  Palette:
    type: object
    properties:
      palette_name:
        type: array
        items:
          $ref: '#/definitions/Color'
  Color:
    type: string
    
responses:
  200:
    description: A list of companies
    schema:
      $ref: '#/definitions/Palette'
    examples:
      rgb: {'companies_id':[1,2,3]}